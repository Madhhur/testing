apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "my-frontend-chart.fullname" . }}
  labels:
    app: {{ include "my-frontend-chart.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "my-frontend-chart.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "my-frontend-chart.fullname" . }}
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        ports:
        - containerPort: 80
